# 基本信息

统一规定：一个时间槽默认为4课时，一天一共有12课时，所以有3个时间槽，1-4表示早上，5-8表示下午，9-12表示晚上，默认一个实验占用一个slot（4学时）

## 1. 学期（`academic_term`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `code` | `VARCHAR(32)` | 是 | `DEMO-MAIN` | code 字段；非空 |
| `academic_year` | `VARCHAR(16)` | 是 | `2025-2026` | academic_year 字段；非空 |
| `semester_no` | `SMALLINT` | 是 | `2` | semester_no 字段；非空 |
| `start_date` | `DATE` | 是 | `2026-02-23` | start_date 字段；非空 |
| `end_date` | `DATE` | 是 | `2026-06-28` | end_date 字段；非空 |
|  |  |  |  | 本学期选课开始时间 |
|  |  |  |  | 本学期选课结束时间 |
|  |  |  |  | 退选结束时间 |
| `total_weeks` | `SMALLINT` | 是 | `1` | total_weeks 字段；非空 |
| `days_per_week` | `SMALLINT` | 是 | `1` | days_per_week 字段；非空 |
| `slots_per_day` | `SMALLINT` | 是 | `1` | slots_per_day 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `created_by` | UUID | 否 |                       | created_by 字段 |
| `updated_by` | UUID | 否 |  | updated_by 字段 |
| `id` | UUIID |  |  | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:08 | updated_at 字段；非空 |

**表级约束**

- 检查：total_weeks >= 1
- 检查：status IN ('DRAFT', 'ACTIVE', 'CLOSED')
- 唯一：academic_year, semester_no
- 检查：semester_no BETWEEN 1 AND 3
- 唯一：code
- 检查：slots_per_day >= 1
- 检查：days_per_week BETWEEN 1 AND 7

## 2. 设备类型（`equipment_type`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `equipment_code` | `VARCHAR(32)` | 是 | `DEMO-EQ-01` | equipment_code 字段；非空 |
| `name` | `VARCHAR(100)` | 是 | `模拟名称`(请用真实物理设备名称) | name 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `created_by` | UUID |  | created_by 字段 |
| `updated_by` | UUID | 否 |  | updated_by 字段 |
| `id` | UUID | 是 |  | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:08` | updated_at 字段；非空 |

**表级约束**

- 唯一：equipment_code
- 检查：status IN ('ACTIVE', 'INACTIVE')

## 3. 实验课程（`experiment_course`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `course_code` | `VARCHAR(32)` | 是 | `DEMO-PHY101` | course_code 字段；非空 |
| `course_name` | `VARCHAR(100)` | 是 | 大学物理实验 | course_name 字段；非空 |
| `credits` | `NUMERIC(4, 1)` | 是 | `DEMO_VALUE` | credits 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `created_by` | UUID | 否 |  | created_by 字段 |
| `updated_by` | UUID            | 否 |  | updated_by 字段 |
| `id` | UUID            | 是 | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

课程找个3门实际课程（如大学物理实验、光学实验基础...）

**表级约束**

- 检查：credits >= 0
- 检查：status IN ('ACTI 'INACTIVE')
- 唯一：course_code

## 4. 专业（`major`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `code` | `VARCHAR(32)` | 是 | `DEMO-MAIN` | code 字段；非空 |
| `name` | `VARCHAR(100)` | 是 | `模拟名称` | name 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `created_by` | UUID | 否 |  | created_by 字段 |
| `updated_by` | UUID | 否 | `` | updated_by 字段 |
| `id` | `UUID` | 是 | `789e529c-5269-5240-849b-46ce4236628a` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 唯一：name
- 唯一：code
- 检查：status IN ('ACTIVE', 'INACTIVE')

## 5. 账号（`user_account`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `login_name` | `VARCHAR(64)` | 是 | `d2024001001` | login_name 字段；非空 |
| `password_hash` | `TEXT` | 是 | `$2b$12$DEMO_HASH` | password_hash 字段；非空 |
| `user_type` | `VARCHAR(20)` | 是 | `DEMO_VALUE` | user_type 字段；非空 |
| `phone` | `VARCHAR(32)` | 否 | `13800000001` | phone 字段 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `password_changed_at` | `TIMESTAMPTZ` | 否 | `NULL` | password_changed_at 字段 |
| `created_by` | UUID | 否 |  | created_by 字段 |
| `updated_by` | UUID | 否 |  | updated_by 字段 |
| `id` | `UUID` | 是 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：user_type IN ('STUDENT', 'TEACHER', 'ADMIN', 'AUDITOR')
- 唯一：login_name
- 检查：status IN ('ACTIVE', 'LOCKED', 'DISABLED')
- 索引：ix_user_account_type_status



说明

​	这里初始密码统一按照123456的哈希值存储



# 学生信息

## 6. 学生（`student`）

| 字段名            | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                  |
| ----------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------ |
| `user_id`         | `UUID`          |  是  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | user_id 字段；非空；外键 → user_account.id |
| `student_no`      | `VARCHAR(32)`   |  是  | `D2024001001`                          | student_no 字段；非空                      |
| `name`            | `VARCHAR(100)`  |  是  | `模拟名称`                             | name 字段；非空                            |
| `gender`          | `VARCHAR(16)`   |  否  | `男`                                   | gender 字段                                |
| `enrollment_year` | `SMALLINT`      |  是  | `2024`                                 | enrollment_year 字段；非空                 |
| `major_id`        | `UUID`          |  是  | `789e529c-5269-5240-849b-46ce4236628a` | major_id 字段；非空；外键 → major.id       |
| `campus`          | VARCHAR(32)     |  是  |                                        | campus_id 字段；非空；外键 → campus.id     |
| `academic_status` | `VARCHAR(20)`   |  是  | `ACTIVE`                               | academic_status 字段；非空                 |
| `created_by`      | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                            |
| `updated_by`      | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                            |
| `id`              | `UUID`          |  是  | `035b38b7-0e8f-589a-a041-37292b2f1e83` | id 字段；主键；非空                        |
| `created_at`      | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                      |
| `updated_at`      | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非                        |



**表级约束**

- 约束以当前 SQLAlchemy 模型为准。



说明

​	campus表示所在校区，包括XX校区/XX校区（一共需要2个校区）



存在问题

​	好像漏了是否通过先修课程，是不是可以加入是否通过潜在先修课程（为了方便可以用所有培养方案用到的所有先修课程通过情况，到时选课根据具体问题具体分析，注意通过1,未通过0）

## 7. 忙闲位图（`student_busy_bitmap`）

| 字段名            | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                   |
| ----------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------- |
| `student_id`      | `UUID`          |  是  | `035b38b7-0e8f-589a-a041-37292b2f1e83` | student_id 字段；非空；外键 → student.id    |
| `term_id`         | `UUID`          |  是  | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `start_week`      | `SMALLINT`      |  是  | `1`                                    | start_week 字段；非空                       |
| `end_week`        | `SMALLINT`      |  是  | `1`                                    | end_week 字段；非空                         |
| `days_per_week`   | `SMALLINT`      |  是  | `1`                                    | days_per_week 字段；非空                    |
| `slots_per_day`   | `SMALLINT`      |  是  | `1`                                    | slots_per_day 字段；非空                    |
| `bitmap`          | `BYTEA`         |  是  | `\x000000000000000f`                   | bitmap 字段；非空                           |
| `mapping_version` | `INTEGER`       |  是  | `1`                                    | mapping_version 字段；非空                  |
| `source_version`  | `VARCHAR(64)`   |  否  | `NULL`                                 | source_version 字段                         |
| `id`              | `UUID`          |  是  | `44ad5bad-a593-54eb-b8cf-86725fff6770` | id 字段；主键；非空                         |
| `created_at`      | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                       |
| `updated_at`      | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                       |

**表级约束**

- 检查：end_week >= start_week
- 检查：days_per_week BETWEEN 1 AND 7
- 检查：slots_per_day >= 1
- 唯一：student_id, term_id, mapping_version
- 检查：start_week >= 1

统一规定：一个时间槽默认为4课时，一天一共有12课时，所以有3个时间槽，1-4表示早上，5-8表示下午，9-12表示晚上

# 教师信息

## 7. 教师（`teacher`）

| 字段名        | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                  |
| ------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------ |
| `user_id`     | `UUID`          |  是  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | user_id 字段；非空；外键 → user_account.id |
| `employee_no` | `VARCHAR(32)`   |  是  | `DEMO-T001`                            | employee_no 字段；非空                     |
| `name`        | `VARCHAR(100)`  |  是  | `模拟名称`                             | name 字段；非空                            |
| `title`       | `VARCHAR(64)`   |  否  | `讲师`                                 | title 字段                                 |
| `status`      | `VARCHAR(20)`   |  是  | `ACTIVE`                               | status 字段；非空                          |
| `created_by`  | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                            |
| `updated_by`  | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                            |
| `id`          | `UUID`          |  是  | `167385db-723a-5b7f-9086-dab410ac17d1` | id 字段；主键；非空                        |
| `created_at`  | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                      |
| `updated_at`  | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                      |

**表级约束**

- 检查：status IN ('ACTIVE', 'LEAVE', 'INACTIVE')
- 唯一：employee_no
- 唯一：user_id
- 索引：ix_teacher_campus_status

## 26. 教师可用时间（`teacher_availability`）

| 字段名              | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                   |
| ------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------- |
| `teacher_id`        | `UUID`          |  是  | `167385db-723a-5b7f-9086-dab410ac17d1` | teacher_id 字段；非空；外键 → teacher.id    |
| `term_id`           | `UUID`          |  是  | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `week_start`        | `SMALLINT`      |  是  | `1`                                    | week_start 字段；非空                       |
| `week_end`          | `SMALLINT`      |  是  | `18`                                   | week_end 字段；非空                         |
| `day_of_week`       | `SMALLINT`      |  是  | `3`                                    | day_of_week 字段；非空                      |
| `start_slot`        | `SMALLINT`      |  是  | `5`                                    | start_slot 字段；非空                       |
| `end_slot`          | `SMALLINT`      |  是  | `8`                                    | end_slot 字段；非空                         |
| `availability_type` | `VARCHAR(20)`   |  是  | `AVAILABLE`                            | availability_type 字段；非空                |
| `reason`            | `TEXT`          |  否  | `NULL`                                 | reason 字段                                 |
| `id`                | `UUID`          |  是  | `2bdf3110-479e-5e23-b5f3-7442e4bf6bc5` | id 字段；主键；非空                         |
| `created_at`        | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                       |
| `updated_at`        | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                       |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

## 27. 教师资质（`teacher_project_qualification`）

| 字段名       | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                           |
| ------------ | --------------- | :--: | -------------------------------------- | --------------------------------------------------- |
| `teacher_id` | `UUID`          |  是  | `167385db-723a-5b7f-9086-dab410ac17d1` | teacher_id 字段；非空；外键 → teacher.id            |
| `project_id` | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id |
| `status`     | `VARCHAR(20)`   |  是  | `ACTIVE`                               | status 字段；非空                                   |
| `created_by` | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                                     |
| `updated_by` | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                                     |
| `id`         | `UUID`          |  是  | `b949dbc8-6375-58f0-956b-e8e6877d5449` | id 字段；主键；非空                                 |
| `created_at` | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                               |
| `updated_at` | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                               |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

# 实验室与实验项目信息

## 8. 实验室（`laboratory`）

| 字段名               | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                  |
| -------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------ |
| `lab_code`           | `VARCHAR(32)`   |  是  | `DEMO-A203`                            | lab_code 字段；非空                        |
| `name`               | `VARCHAR(100)`  |  是  | `模拟名称`                             | name 字段；非空                            |
| `room_type`          | `VARCHAR(64)`   |  否  | `NULL`                                 | room_type 字段                             |
| `safety_capacity`    | `INTEGER`       |  是  | `40`                                   | safety_capacity 字段；非空                 |
| `manager_teacher_id` | `UUID`          |  否  | `167385db-723a-5b7f-9086-dab410ac17d1` | manager_teacher_id 字段；外键 → teacher.id |
| `status`             | `VARCHAR(20)`   |  是  | `ACTIVE`                               | status 字段；非空                          |
| `created_by`         | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                            |
| `updated_by`         | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                            |
| `id`                 | `UUID`          |  是  | `a57a74bf-5b52-56b6-b6ea-5123a0e866e4` | id 字段；主键；非空                        |
| `created_at`         | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                      |
| `updated_at`         | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                      |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

## 9. 实验项目（`experiment_project`）

| 字段名                       | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                         |
| ---------------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------------- |
| `project_code`               | `VARCHAR(32)`   |  是  | `DEMO-MECH-01`                         | project_code 字段；非空                           |
| `course_id`                  | `UUID`          |  是  | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | course_id 字段；非空；外键 → experiment_course.id |
| `project_name`               | `VARCHAR(150)`  |  是  | `单摆实验（模拟）`                     | project_name 字段；非空                           |
| `category`                   | `VARCHAR(20)`   |  是  | `MECHANICS`                            | category 字段；非空                               |
| `required_slots`             | `SMALLINT`      |  是  | 4                                      | required_slots为4表示当次项目4节连堂              |
| `material_note`              | `TEXT`          |  否  | `NULL`                                 | 实验需要用到的器材                                |
| `historical_selection_ratio` | `NUMERIC(6, 4)` |  是  | `DEMO_VALUE`                           | historical_selection_ratio 字段；非空             |
| `status`                     | `VARCHAR(20)`   |  是  | `ACTIVE`                               | status 字段；非空                                 |
| `created_by`                 | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                                   |
| `updated_by`                 | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                                   |
| `id`                         | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | id 字段；主键；非空                               |
| `created_at`                 | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                             |
| `updated_at`                 | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                             |
|                              | VAR（32）       |      |                                        | 开课校区                                          |

**表级约束**

- 检查：category IN ('BASIC', 'MECHANICS', 'ELECTRICITY', 'OPTICS', 'MODERN', 'OTHER')
- 检查：required_slots >= 1
- 检查：default_group_size >= 1
- 检查：historical_selection_ratio BETWEEN 0 AND 1
- 检查：status IN ('ACTIVE', 'INACTIVE')
- 唯一：project_code
- 唯一：course_id, project_name
- 索引：ix_experiment_project_course_status

## 10. 项目设备要求（`project_equipment_requirement`）

| 字段名              | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                              |
| ------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------------------ |
| `project_id`        | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id    |
| `equipment_type_id` | `UUID`          |  是  | `370c5db0-3836-5a06-8e25-39adc4f07ff3` | equipment_type_id 字段；非空；外键 → equipment_type.id |
| `units_per_group`   | `SMALLINT`      |  是  | `1`                                    | units_per_group 字段；非空                             |
| `required`          | `BOOLEAN`       |  是  | `TRUE`                                 | required 字段；非空                                    |
| `description`       | `TEXT`          |  否  | `NULL`                                 | description 字段                                       |
| `id`                | `UUID`          |  是  | `a8b7b3c2-867d-5623-96f9-a9c909705f92` | id 字段；主键；非空                                    |
| `created_at`        | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                                  |
| `updated_at`        | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                                  |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

## 11. 实验室设备数量（`lab_equipment_inventory`）

| 字段名              | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                              |
| ------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------------------ |
| `laboratory_id`     | `UUID`          |  是  | `a57a74bf-5b52-56b6-b6ea-5123a0e866e4` | laboratory_id 字段；非空；外键 → laboratory.id         |
| `equipment_type_id` | `UUID`          |  是  | `370c5db0-3836-5a06-8e25-39adc4f07ff3` | equipment_type_id 字段；非空；外键 → equipment_type.id |
| `total_quantity`    | `INTEGER`       |  是  | `1`                                    | total_quantity 字段；非空                              |
| `usable_quantity`   | `INTEGER`       |  是  | `1`                                    | usable_quantity 字段；非空                             |
| `disabled_quantity` | `INTEGER`       |  是  | `1`                                    | disabled_quantity 字段；非空                           |
| `checked_at`        | `TIMESTAMPTZ`   |  否  | `NULL`                                 | checked_at 字段                                        |
| `created_by`        | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                                        |
| `updated_by`        | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                                        |
| `id`                | `UUID`          |  是  | `45a7f98d-16ee-592b-9eab-08aeef0c4799` | id 字段；主键；非空                                    |
| `created_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                                  |
| `updated_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                                  |

**表级约束**

- 检查：total_quantity >= 0
- 检查：disabled_quantity >= 0
- 检查：usable_quantity + disabled_quantity <= total_quantity
- 唯一：laboratory_id, equipment_type_id
- 检查：usable_quantity >= 0







# 培养方案先验知识

## 11. 培养方案（`training_plan`）

| 字段名            | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                 |
| ----------------- | --------------- | :--: | -------------------------------------- | ----------------------------------------- |
| `plan_code`       | `VARCHAR(64)`   |  是  | `DEMO-PLAN-2024-V1`                    | plan_code 字段；非空                      |
| `major_id`        | `UUID`          |  是  | `789e529c-5269-5240-849b-46ce4236628a` | major_id 字段；非空；外键 → major.id      |
| `enrollment_year` | `SMALLINT`      |  是  | `2024`                                 | enrollment_year 字段；非空                |
| `version_no`      | `SMALLINT`      |  是  | `1`                                    | version_no 字段；非空                     |
| `status`          | `VARCHAR(20)`   |  是  | `PUBLISHED`                            | status 字段；非空                         |
| `published_at`    | `TIMESTAMPTZ`   |  否  | `NULL`                                 | published_at 字段                         |
| `published_by`    | `UUID`          |  否  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | published_by 字段；外键 → user_account.id |
| `created_by`      | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                           |
| `updated_by`      | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                           |
| `id`              | `UUID`          |  是  | `148c0c22-cc1f-541c-9272-b6847f475cc0` | id 字段；主键；非空                       |
| `created_at`      | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                     |
| `updated_at`      | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                     |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

## 12. 方案课程（`training_plan_course`）

| 字段名                       | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                         |
| ---------------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------------- |
| `plan_id`                    | `UUID`          |  是  | `148c0c22-cc1f-541c-9272-b6847f475cc0` | plan_id 字段；非空；外键 → training_plan.id       |
| `course_id`                  | `UUID`          |  是  | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | course_id 字段；非空；外键 → experiment_course.id |
| `course_nature`              | `VARCHAR(20)`   |  是  | `REQUIRED`                             | course_nature 字段；非空                          |
| `study_year`                 | `SMALLINT`      |  是  | `2`                                    | study_year 字段；非空                             |
| `semester_no`                | `SMALLINT`      |  是  | `2`                                    | semester_no 字段；非空                            |
| `required_project_count`     | `SMALLINT`      |  是  | `4`                                    | required_project_count 字段；非空                 |
| `optional_project_min_count` | `SMALLINT`      |  是  | `2`                                    | optional_project_min_count 字段；非空             |
| `order_rule_text`            | `TEXT`          |  否  | `NULL`                                 | order_rule_text 字段                              |
| `created_by`                 | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                                   |
| `updated_by`                 | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                                   |
| `id`                         | `UUID`          |  是  | `9ed36cce-0609-55b6-82c2-be011796f1e4` | id 字段；主键；非空                               |
| `created_at`                 | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                             |
| `updated_at`                 | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                             |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

  **注意**

  课程先修以课程为准，不以项目为准

## 13. 课程先修（`course_prerequisite`）

| 字段名                   | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                                    |
| ------------------------ | --------------- | :--: | -------------------------------------- | ------------------------------------------------------------ |
| `plan_course_id`         | `UUID`          |  是  | `9ed36cce-0609-55b6-82c2-be011796f1e4` | plan_course_id 字段；非空；外键 → training_plan_course.id    |
| `prerequisite_course_id` | `UUID`          |  是  | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | prerequisite_course_id 字段；非空；外键 → experiment_course.id |
| `requirement_type`       | `VARCHAR(20)`   |  是  | `REQUIRED`                             | requirement_type 字段；非空                                  |
| `description`            | `TEXT`          |  否  | `NULL`                                 | description 字段                                             |
| `id`                     | `UUID`          |  是  | `cbce0989-a8cd-5746-88f3-fccde8ca4f2c` | id 字段；主键；非空                                          |
| `created_at`             | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                                        |
| `updated_at`             | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                                        |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。



## 14. 项目顺序（`project_order_constraint`）

| 字段名              | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                                  |
| ------------------- | --------------- | :--: | -------------------------------------- | ---------------------------------------------------------- |
| `plan_course_id`    | `UUID`          |  是  | `9ed36cce-0609-55b6-82c2-be011796f1e4` | plan_course_id 字段；非空；外键 → training_plan_course.id  |
| `before_project_id` | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | before_project_id 字段；非空；外键 → experiment_project.id |
| `after_project_id`  | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | after_project_id 字段；非空；外键 → experiment_project.id  |
| `allow_override`    | `BOOLEAN`       |  是  | `TRUE`                                 | allow_override 字段；非空                                  |
| `description`       | `TEXT`          |  否  | `模拟数据说明`                         | description 字段                                           |
| `id`                | `UUID`          |  是  | `cf9ea2bf-a0c2-5faa-835a-92be6020de40` | id 字段；主键；非空                                        |
| `created_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                                      |
| `updated_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                                      |

**表级约束**

- 唯一：plan_course_id, before_project_id, after_project_id
- 检查：before_project_id <> after_project_id

## 15. 方案项目（`training_plan_project`）

| 字段名             | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                                 |
| ------------------ | --------------- | :--: | -------------------------------------- | --------------------------------------------------------- |
| `plan_course_id`   | `UUID`          |  是  | `9ed36cce-0609-55b6-82c2-be011796f1e4` | plan_course_id 字段；非空；外键 → training_plan_course.id |
| `project_id`       | `UUID`          |  是  | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id       |
| `requirement_type` | `VARCHAR(20)`   |  是  | `REQUIRED`                             | requirement_type 字段；非空                               |
| `display_order`    | `SMALLINT`      |  是  | `1`                                    | display_order 字段；非空                                  |
| `id`               | `UUID`          |  是  | `052a740d-40e9-522e-a968-6baf401d3b4b` | id 字段；主键；非空                                       |
| `created_at`       | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                                     |
| `updated_at`       | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                                     |

**表级约束**

- 唯一：plan_course_id, project_id
- 检查：requirement_type IN ('REQUIRED', 'OPTIONAL')



# 系统端数据存储

## 8. 通知（`notification`）

| 字段名              | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                            |
| ------------------- | --------------- | :--: | -------------------------------------- | ---------------------------------------------------- |
| `recipient_user_id` | `UUID`          |  是  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | recipient_user_id 字段；非空；外键 → user_account.id |
| `notification_type` | `VARCHAR(20)`   |  是  | `DEMO_VALUE`                           | notification_type 字段；非空                         |
| `title`             | `VARCHAR(200)`  |  是  | `换组申请审核结果`                     | title 字段；非空                                     |
| `content`           | `TEXT`          |  是  | `模拟通知内容`                         | content 字段；非空                                   |
| `business_type`     | `VARCHAR(64)`   |  否  | `APPLICATION`                          | business_type 字段                                   |
| `business_id`       | `UUID`          |  否  | `NULL`                                 | business_id 字段                                     |
| `status`            | `VARCHAR(20)`   |  是  | `READ`                                 | status 字段；非空                                    |
| `read_at`           | `TIMESTAMPTZ`   |  否  | `NULL`                                 | read_at 字段                                         |
| `sent_at`           | `TIMESTAMPTZ`   |  否  | `NULL`                                 | sent_at 字段                                         |
| `id`                | `UUID`          |  是  | `eb80349d-8d6a-5025-8f93-6e3bff51d402` | id 字段；主键；非空                                  |
| `created_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                                |
| `updated_at`        | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                                |

**表级约束**

- 检查：notification_type IN ('SYSTEM', 'SELECTION', 'APPLICATION', 'SCHEDULE', 'RESOURCE')
- 检查：status IN ('PENDING', 'SENT', 'READ', 'FAILED')
- 索引：ix_notification_recipient_status







# 智能体评估数据

## 7. 智能体运行（`agent_run`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `thread_id` | `VARCHAR(128)` | 是 | `demo-thread-1` | thread_id 字段；非空 |
| `graph_name` | `VARCHAR(100)` | 是 | `student_graph` | graph_name 字段；非空 |
| `graph_version` | `VARCHAR(32)` | 是 | `v1` | graph_version 字段；非空 |
| `requester_user_id` | `UUID` | 是 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | requester_user_id 字段；非空；外键 → user_account.id |
| `business_type` | `VARCHAR(64)` | 否 | `APPLICATION` | business_type 字段 |
| `business_id` | `UUID` | 否 | `NULL` | business_id 字段 |
| `prompt_versions` | `JSONB` | 是 | <code>{"demo": true}</code> | prompt_versions 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `SUCCEEDED` | status 字段；非空 |
| `started_at` | `TIMESTAMPTZ` | 否 | `NULL` | started_at 字段 |
| `finished_at` | `TIMESTAMPTZ` | 否 | `NULL` | finished_at 字段 |
| `error_code` | `VARCHAR(64)` | 否 | `NULL` | error_code 字段 |
| `error_message` | `TEXT` | 否 | `NULL` | error_message 字段 |
| `id` | `UUID` | 是 | `98dfb180-2e39-5331-80e1-7a213953a44b` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 唯一：thread_id
- 检查：status IN ('PENDING', 'RUNNING', 'INTERRUPTED', 'SUCCEEDED', 'FAILED', 'CANCELLED')
- 索引：ix_agent_run_requester_status



## 17. 用户反馈（`agent_feedback`）

| 字段名          | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                  |
| --------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------ |
| `run_id`        | `UUID`          |  是  | `98dfb180-2e39-5331-80e1-7a213953a44b` | run_id 字段；非空；外键 → agent_run.id     |
| `user_id`       | `UUID`          |  是  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | user_id 字段；非空；外键 → user_account.id |
| `rating`        | `INTEGER`       |  是  | `5`                                    | rating 字段；非空                          |
| `feedback_type` | `VARCHAR(64)`   |  否  | `ACCURACY`                             | feedback_type 字段                         |
| `comment`       | `TEXT`          |  否  | `模拟评价`                             | comment 字段                               |
| `id`            | `UUID`          |  是  | `c6d69553-0964-525a-ad53-860f74b22b1a` | id 字段；主键；非空                        |
| `created_at`    | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                      |
| `updated_at`    | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                      |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

## 18. 步骤日志（`agent_step_log`）

| 字段名           | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                              |
| ---------------- | --------------- | :--: | -------------------------------------- | -------------------------------------- |
| `run_id`         | `UUID`          |  是  | `98dfb180-2e39-5331-80e1-7a213953a44b` | run_id 字段；非空；外键 → agent_run.id |
| `node_name`      | `VARCHAR(100)`  |  是  | `validate_selection`                   | node_name 字段；非空                   |
| `agent_code`     | `VARCHAR(64)`   |  否  | `student_advisor`                      | agent_code 字段                        |
| `input_summary`  | `JSONB`         |  是  | <code>{"demo":true}</code>             | input_summary 字段；非空               |
| `tool_calls`     | `JSONB`         |  是  | <code>{"demo":true}</code>             | tool_calls 字段；非空                  |
| `output_summary` | `JSONB`         |  是  | <code>{"demo":true}</code>             | output_summary 字段；非空              |
| `latency_ms`     | `INTEGER`       |  否  | `182`                                  | latency_ms 字段                        |
| `token_usage`    | `JSONB`         |  是  | <code>{"demo":true}</code>             | token_usage 字段；非空                 |
| `status`         | `VARCHAR(20)`   |  是  | `SUCCEEDED`                            | status 字段；非空                      |
| `error_message`  | `TEXT`          |  否  | `NULL`                                 | error_message 字段                     |
| `id`             | `UUID`          |  是  | `d7e54571-f611-569e-85da-30ed63627a5c` | id 字段；主键；非空                    |
| `created_at`     | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                  |
| `updated_at`     | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                  |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。



## 20. 操作日志（`operation_log`）

| 字段名             | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                     |
| ------------------ | --------------- | :--: | -------------------------------------- | --------------------------------------------- |
| `operator_user_id` | `UUID`          |  否  | `494ddc8d-8954-558c-a7c2-1d6625a67038` | operator_user_id 字段；外键 → user_account.id |
| `operation_type`   | `VARCHAR(64)`   |  是  | `PUBLISH`                              | operation_type 字段；非空                     |
| `object_type`      | `VARCHAR(64)`   |  是  | `schedule_version`                     | object_type 字段；非空                        |
| `object_id`        | `UUID`          |  否  | `NULL`                                 | object_id 字段                                |
| `request_id`       | `VARCHAR(64)`   |  否  | `NULL`                                 | request_id 字段                               |
| `before_snapshot`  | `JSONB`         |  是  | <code>{"demo":true}</code>             | before_snapshot 字段；非空                    |
| `after_snapshot`   | `JSONB`         |  是  | <code>{"demo":true}</code>             | after_snapshot 字段；非空                     |
| `rule_set_id`      | `UUID`          |  否  | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | rule_set_id 字段；外键 → rule_set.id          |
| `result`           | `VARCHAR(32)`   |  是  | `SUCCEEDED`                            | result 字段；非空                             |
| `ip_address`       | `INET`          |  否  | `192.0.2.10`                           | ip_address 字段                               |
| `id`               | `UUID`          |  是  | `0af592f3-f008-501d-af3b-dadfb8944913` | id 字段；主键；非空                           |
| `created_at`       | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                         |
| `updated_at`       | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                         |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。



# 先验规则

## 11. 规则集（`rule_set`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `rule_domain` | `VARCHAR(20)` | 是 | `SCHEDULING` | 业务域：排课、选课、调整或审批 |
| `rule_set_code` | `VARCHAR(64)` | 是 | `DEMO-PHYSICS-LAB` | rule_set_code 字段；非空 |
| `version_no` | `INTEGER` | 是 | `1` | version_no 字段；非空 |
| `name` | `VARCHAR(150)` | 是 | `模拟名称` | name 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `PUBLISHED` | status 字段；非空 |
| `published_at` | `TIMESTAMPTZ` | 否 | `2026-02-20 10:00:00+08` | published_at 字段 |
| `published_by` | `UUID` | 否 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | published_by 字段；外键 → user_account.id |
| `created_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段 |
| `updated_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段 |
| `id` | `UUID` | 是 | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：status IN ('DRAFT', 'PUBLISHED', 'ARCHIVED')
- 检查：rule_domain IN ('SCHEDULING', 'SELECTION', 'ADJUSTMENT', 'APPROVAL')
- 唯一：rule_domain, rule_set_code, version_no
- 唯一索引：uq_rule_set_published（同域同代码最多一个已发布版本）

内容

​	**每个规则集只保存一个业务域的规则，并独立维护版本。**

## 23. 规则配置（`rule_config`）

| 字段名             | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                  |
| ------------------ | --------------- | :--: | -------------------------------------- | ------------------------------------------ |
| `rule_set_id`      | `UUID`          |  是  | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | rule_set_id 字段；非空；外键 → rule_set.id |
| `rule_code`        | `VARCHAR(64)`   |  是  | `HARD-001`                             | rule_code 字段；非空                       |
| `rule_name`        | `VARCHAR(150)`  |  是  | `时间不得冲突`                         | rule_name 字段；非空                       |
| `enforcement_type` | `VARCHAR(20)`   |  是  | `BLOCK`                                | 执行方式：BLOCK、SCORE、WARN 或 ROUTE      |
| `scope_config`     | `JSONB`         |  是  | <code>{"demo":true}</code>             | scope_config 字段；非空                    |
| `condition_config` | `JSONB`         |  是  | <code>{"demo":true}</code>             | condition_config 字段；非空                |
| `action_config`    | `JSONB`         |  是  | <code>{"demo":true}</code>             | action_config 字段；非空                   |
| `weight`           | `NUMERIC(8, 4)` |  是  | `0.0000`                               | weight 字段；非空                          |
| `priority`         | `INTEGER`       |  是  | `100`                                  | priority 字段；非空                        |
| `description`      | `TEXT`          |  是  | `DEMO_VALUE`                           | description 字段；非空                     |
| `enabled`          | `BOOLEAN`       |  是  | `TRUE`                                 | enabled 字段；非空                         |
| `created_by`       | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                            |
| `updated_by`       | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                            |
| `id`               | `UUID`          |  是  | `cdb3cf28-52c7-528c-8b2e-2c55e5788da6` | id 字段；主键；非空                        |
| `created_at`       | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                      |
| `updated_at`       | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                      |

**表级约束**

- 检查：enforcement_type IN ('BLOCK', 'SCORE', 'WARN', 'ROUTE')
- 检查：非 SCORE 规则的 weight 必须为 0
- 唯一：rule_set_id, rule_code

## 24. 排课任务（`schedule_job`）

| 字段名           | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                   |
| ---------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------- |
| `term_id`        | `UUID`          |  是  | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `task_id`        | `UUID`          |  否  | `a6a1717e-d99c-5a66-8432-795b959eb3f2` | task_id 字段；外键 → teaching_task.id       |
| `job_type`       | `VARCHAR(32)`   |  是  | `INITIAL`                              | job_type 字段；非空                         |
| `status`         | `VARCHAR(20)`   |  是  | `SUCCEEDED`                            | status 字段；非空                           |
| `scheduling_rule_set_id` | `UUID`   |  是  | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | 排课规则集；非空；外键 → rule_set.id        |
| `input_snapshot` | `JSONB`         |  是  | <code>{"demo":true}</code>             | input_snapshot 字段；非空                   |
| `progress`       | `INTEGER`       |  是  | `100`                                  | progress 字段；非空                         |
| `started_at`     | `TIMESTAMPTZ`   |  否  | `NULL`                                 | started_at 字段                             |
| `finished_at`    | `TIMESTAMPTZ`   |  否  | `NULL`                                 | finished_at 字段                            |
| `error_code`     | `VARCHAR(64)`   |  否  | `NULL`                                 | error_code 字段                             |
| `error_message`  | `TEXT`          |  否  | `NULL`                                 | error_message 字段                          |
| `created_by`     | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                             |
| `updated_by`     | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                             |
| `id`             | `UUID`          |  是  | `94c58170-686e-54ac-b91b-bb9c7343a7ec` | id 字段；主键；非空                         |
| `created_at`     | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | created_at 字段；非空                       |
| `updated_at`     | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | updated_at 字段；非空                       |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。

# 人数需求

## 15. 教学任务（`teaching_task`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `task_code` | `VARCHAR(64)` | 是 | `DEMO-TASK-PHY101` | task_code 字段；非空 |
| `term_id` | `UUID` | 是 | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `course_id` | `UUID` | 是 | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | course_id 字段；非空；外键 → experiment_course.id |
| `planned_student_count` | `INTEGER` | 是 | `1` | planned_student_count 字段；非空 |
| `week_start` | `SMALLINT` | 是 | `1` | week_start 字段；非空 |
| `requirement_type` | `VARCHAR(20)` | 是 | `REQUIRED` | requirement_type 字段；非空 |
| `description` | `TEXT` | 否 | `模拟数据说明` | description 字段 |
| `id` | `UUID` | 是 | `cbce0989-a8cd-5746-88f3-fccde8ca4f2c` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：requirement_type IN ('MUST_COMPLETE', 'MUST_ENROLL', 'ADVISORY')
- 唯一：plan_course_id, prerequisite_course_id

## 21. 项目需求（`project_demand`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `task_id` | `UUID` | 是 | `a6a1717e-d99c-5a66-8432-795b959eb3f2` | task_id 字段；非空；外键 → teaching_task.id |
| `project_id` | `UUID` | 是 | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id |
| `requirement_type` | `VARCHAR(20)` | 是 | `REQUIRED` | requirement_type 字段；非空 |
| `base_demand` | `INTEGER` | 是 | `2000` | base_demand 字段；非空 |
| `prediction_ratio` | `NUMERIC(6, 4)` | 是 | `1.0000` | prediction_ratio 字段；非空 |
| `buffer_ratio` | `NUMERIC(5, 2)` | 是 | `1.20` | buffer_ratio 字段；非空 |
| `required_capacity` | `INTEGER` | 是 | `2400` | required_capacity 字段；非空 |
| `required_session_count` | `INTEGER` | 是 | `67` | required_session_count 字段；非空 |
| `calculation_snapshot` | `JSONB` | 是 | <code>{"demo":true}</code> | calculation_snapshot 字段；非空 |
| `id` | `UUID` | 是 | `48a77d99-a096-5617-b5c3-0959b5e6f53c` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `DEMO_VALUE` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `DEMO_VALUE` | updated_at 字段；非空 |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。





## 28. 覆盖群体（`teaching_task_cohort`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `task_id` | `UUID` | 是 | `a6a1717e-d99c-5a66-8432-795b959eb3f2` | task_id 字段；非空；外键 → teaching_task.id |
| `major_id` | `UUID` | 是 | `789e529c-5269-5240-849b-46ce4236628a` | major_id 字段；非空；外键 → major.id |
| `enrollment_year` | `SMALLINT` | 是 | `2024` | enrollment_year 字段；非空 |
| `student_count` | `INTEGER` | 是 | `200` | student_count 字段；非空 |
| `id` | `UUID` | 是 | `c5f22ec7-095b-50e8-a7a9-52d32c862d25` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `DEMO_VALUE` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `DEMO_VALUE` | updated_at 字段；非空 |

**表级约束**

- 约束以当前 SQLAlchemy 模型为准。



## 32. 实验室项目能力（`lab_project_capability`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `laboratory_id` | `UUID` | 是 | `a57a74bf-5b52-56b6-b6ea-5123a0e866e4` | laboratory_id 字段；非空；外键 → laboratory.id |
| `project_id` | `UUID` | 是 | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id |
| `effective_capacity` | `INTEGER` | 是 | `1` | effective_capacity 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `ACTIVE` | status 字段；非空 |
| `note` | `TEXT` | 否 | `NULL` | note 字段 |
| `id` | `UUID` | 是 | `b96a95aa-c6b5-58e4-903a-8c16340c2047` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：status IN ('ACTIVE', 'SUSPENDED', 'INACTIVE')
- 唯一：laboratory_id, project_id
- 检查：effective_capacity >= 1



# 选课结果信息

## 33. 选课窗口（`selection_window`）

| 字段名 | PostgreSQL 类型 | 必填 | 含义/约束 |
|---|---|:---:|---|
| `term_id` | `UUID` | 是 | 学期；外键 → academic_term.id |
| `course_id` | `UUID` | 否 | 课程；外键 → experiment_course.id |
| `selection_rule_set_id` | `UUID` | 是 | 选课规则集；外键 → rule_set.id |
| `start_at` | `TIMESTAMPTZ` | 是 | 选课开始时间 |
| `end_at` | `TIMESTAMPTZ` | 是 | 选课结束时间 |
| `withdraw_end_at` | `TIMESTAMPTZ` | 否 | 退选截止时间 |
| `status` | `VARCHAR(20)` | 是 | DRAFT、OPEN 或 CLOSED |

`selection_rule_set_id` 必须引用 `SELECTION` 业务域，数据库触发器会拒绝跨域引用。

## 34. 资源异常（`resource_issue_report`）

| 字段名                | PostgreSQL 类型 | 必填 | 范例值                                 | 含义/约束                                         |
| --------------------- | --------------- | :--: | -------------------------------------- | ------------------------------------------------- |
| `report_no`           | `VARCHAR(64)`   |  是  | `DEMO_VALUE`                           | report_no 字段；非空                              |
| `reporter_teacher_id` | `UUID`          |  是  | `167385db-723a-5b7f-9086-dab410ac17d1` | reporter_teacher_id 字段；非空；外键 → teacher.id |
| `issue_type`          | `VARCHAR(32)`   |  是  | `EQUIPMENT_FAILURE`                    | issue_type 字段；非空                             |
| `laboratory_id`       | `UUID`          |  是  | `a57a74bf-5b52-56b6-b6ea-5123a0e866e4` | laboratory_id 字段；非空；外键 → laboratory.id    |
| `project_id`          | `UUID`          |  否  | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；外键 → experiment_project.id     |
| `equipment_type_id`   | `UUID`          |  否  | `370c5db0-3836-5a06-8e25-39adc4f07ff3` | equipment_type_id 字段；外键 → equipment_type.id  |
| `impact_start`        | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | impact_start 字段；非空                           |
| `impact_end`          | `TIMESTAMPTZ`   |  是  | `DEMO_VALUE`                           | impact_end 字段；非空                             |
| `severity`            | `VARCHAR(20)`   |  是  | `DEMO_VALUE`                           | severity 字段；非空                               |
| `description`         | `TEXT`          |  是  | `模拟数据说明`                         | description 字段；非空                            |
| `status`              | `VARCHAR(20)`   |  是  | `ACTIVE`                               | status 字段；非空                                 |
| `resolved_at`         | `TIMESTAMPTZ`   |  否  | `NULL`                                 | resolved_at 字段                                  |
| `resolution_note`     | `TEXT`          |  否  | `NULL`                                 | resolution_note 字段                              |
| `created_by`          | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段                                   |
| `updated_by`          | `UUID`          |  否  | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段                                   |
| `id`                  | `UUID`          |  是  | `d3501d7c-bd58-547a-8cb8-bc9728d47af8` | id 字段；主键；非空                               |
| `created_at`          | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | created_at 字段；非空                             |
| `updated_at`          | `TIMESTAMPTZ`   |  是  | `2026-02-20 09:00:00+08`               | updated_at 字段；非空                             |

**表级约束**

- 唯一：report_no
- 检查：status IN ('REPORTED', 'PROCESSING', 'RESOLVED', 'CLOSED')
- 检查：severity IN ('LOW', 'NORMAL', 'HIGH', 'CRITICAL')
- 检查：impact_end > impact_start
- 检查：issue_type IN ('EQUIPMENT_FAILURE', 'MATERIAL_SHORTAGE', 'LAB_UNAVAILABLE', 'ENVIRONMENT', 'OTHER')
- 索引：ix_resource_issue_time



## 35. 课表版本（`schedule_version`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `term_id` | `UUID` | 是 | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `version_no` | `INTEGER` | 是 | `1` | version_no 字段；非空 |
| `parent_version_id` | `UUID` | 否 | `c5005ec5-a452-5d41-b154-cc39cd611f97` | parent_version_id 字段；外键 → schedule_version.id |
| `source_job_id` | `UUID` | 否 | `94c58170-686e-54ac-b91b-bb9c7343a7ec` | source_job_id 字段；外键 → schedule_job.id |
| `status` | `VARCHAR(20)` | 是 | `PUBLISHED` | status 字段；非空 |
| `hard_constraint_passed` | `BOOLEAN` | 是 | `TRUE` | hard_constraint_passed 字段；非空 |
| `soft_score` | `NUMERIC(12, 4)` | 否 | `NULL` | soft_score 字段 |
| `score_details` | `JSONB` | 是 | <code>{"demo": true}</code> | score_details 字段；非空 |
| `optimization_params` | `JSONB` | 是 | <code>{"demo": true}</code> | optimization_params 字段；非空 |
| `scheduling_rule_set_id` | `UUID` | 是 | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | 排课规则集；非空；外键 → rule_set.id |
| `published_by` | `UUID` | 否 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | published_by 字段；外键 → user_account.id |
| `published_at` | `TIMESTAMPTZ` | 否 | `2026-02-20 10:00:00+08` | published_at 字段 |
| `created_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段 |
| `updated_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段 |
| `id` | `UUID` | 是 | `c5005ec5-a452-5d41-b154-cc39cd611f97` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 唯一：term_id, version_no
- 检查：status IN ('DRAFT', 'CANDIDATE', 'PUBLISHED', 'ARCHIVED')
- 唯一索引：uq_schedule_version_published



## 38. 实验场次（`experiment_session`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `schedule_version_id` | `UUID` | 是 | `c5005ec5-a452-5d41-b154-cc39cd611f97` | schedule_version_id 字段；非空；外键 → schedule_version.id |
| `session_code` | `VARCHAR(64)` | 是 | `DEMO-W03-WED-05` | session_code 字段；非空 |
| `task_id` | `UUID` | 是 | `a6a1717e-d99c-5a66-8432-795b959eb3f2` | task_id 字段；非空；外键 → teaching_task.id |
| `project_id` | `UUID` | 是 | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id |
| `week_no` | `SMALLINT` | 是 | `1` | week_no 字段；非空 |
| `day_of_week` | `SMALLINT` | 是 | `1` | day_of_week 字段；非空 |
| `start_slot` | `SMALLINT` | 是 | `1` | start_slot 字段；非空 |
| `end_slot` | `SMALLINT` | 是 | `1` | end_slot 字段；非空 |
| `teacher_id` | `UUID` | 是 | `167385db-723a-5b7f-9086-dab410ac17d1` | teacher_id 字段；非空；外键 → teacher.id |
| `laboratory_id` | `UUID` | 是 | `a57a74bf-5b52-56b6-b6ea-5123a0e866e4` | laboratory_id 字段；非空；外键 → laboratory.id |
| `capacity` | `INTEGER` | 是 | `1` | capacity 字段；非空 |
| `selected_count` | `INTEGER` | 是 | `1` | selected_count 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `OPEN` | status 字段；非空 |
| `locked` | `BOOLEAN` | 是 | `TRUE` | locked 字段；非空 |
| `created_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段 |
| `updated_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段 |
| `id` | `UUID` | 是 | `76b767a6-622e-5c4e-89e1-8127b6094772` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：selected_count >= 0
- 检查：status IN ('DRAFT', 'OPEN', 'FULL', 'SUSPENDED', 'CANCELLED', 'COMPLETED')
- 检查：day_of_week BETWEEN 1 AND 7
- 检查：selected_count <= capacity
- 唯一：schedule_version_id, session_code
- 检查：week_no >= 1
- 检查：start_slot >= 1
- 检查：end_slot >= start_slot
- 检查：capacity >= 1
- 索引：ix_experiment_session_time
- 索引：ix_experiment_session_project_status

## 39. 申请单（`application_request`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `request_no` | `VARCHAR(64)` | 是 | `DEMO-APP-0001` | request_no 字段；非空 |
| `request_type` | `VARCHAR(32)` | 是 | `RESCHEDULE` | request_type 字段；非空 |
| `applicant_user_id` | `UUID` | 是 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | applicant_user_id 字段；非空；外键 → user_account.id |
| `student_id` | `UUID` | 否 | `035b38b7-0e8f-589a-a041-37292b2f1e83` | student_id 字段；外键 → student.id |
| `teacher_id` | `UUID` | 否 | `167385db-723a-5b7f-9086-dab410ac17d1` | teacher_id 字段；外键 → teacher.id |
| `project_id` | `UUID` | 否 | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；外键 → experiment_project.id |
| `target_project_id` | `UUID` | 否 | `1e20a175-3274-51ff-8473-023635609404` | target_project_id 字段；外键 → experiment_project.id |
| `original_session_id` | `UUID` | 否 | `76b767a6-622e-5c4e-89e1-8127b6094772` | original_session_id 字段；外键 → experiment_session.id |
| `target_session_id` | `UUID` | 否 | `76b767a6-622e-5c4e-89e1-8127b6094772` | target_session_id 字段；外键 → experiment_session.id |
| `reason` | `TEXT` | 是 | `DEMO_VALUE` | reason 字段；非空 |
| `payload` | `JSONB` | 是 | <code>{"demo": true}</code> | payload 字段；非空 |
| `validation_result` | `JSONB` | 是 | <code>{"demo": true}</code> | validation_result 字段；非空 |
| `adjustment_rule_set_id` | `UUID` | 否 | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | 调整规则集；外键 → rule_set.id |
| `approval_rule_set_id` | `UUID` | 否 | `bc7a536c-1e13-5ad6-8219-614ca4fdc0b7` | 审批规则集；外键 → rule_set.id |
| `status` | `VARCHAR(20)` | 是 | `EXECUTED` | status 字段；非空 |
| `submitted_at` | `TIMESTAMPTZ` | 否 | `NULL` | submitted_at 字段 |
| `executed_at` | `TIMESTAMPTZ` | 否 | `NULL` | executed_at 字段 |
| `created_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段 |
| `updated_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段 |
| `id` | `UUID` | 是 | `51ad1c3f-2b54-5785-8fda-45e8b22014c0` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 唯一：request_no
- 检查：request_type IN ('RESCHEDULE', 'MAKEUP', 'PROJECT_CHANGE', 'TEACHER_ADJUSTMENT', 'LAB_CHANGE', 'RESOURCE_ADJUSTMENT')
- 检查：status IN ('DRAFT', 'SUBMITTED', 'VALIDATING', 'PENDING_REVIEW', 'APPROVED', 'REJECTED', 'EXECUTED', 'FAILED', 'CANCELLED')
- 索引：ix_application_request_type_status
- 索引：ix_application_request_applicant_status

## 40. 学生项目记录（`student_project_record`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `student_id` | `UUID` | 是 | `035b38b7-0e8f-589a-a041-37292b2f1e83` | student_id 字段；非空；外键 → student.id |
| `term_id` | `UUID` | 是 | `831ccfd4-5e76-5238-8e37-af0c792e9252` | term_id 字段；非空；外键 → academic_term.id |
| `course_id` | `UUID` | 是 | `2c317f72-e897-58c8-88a8-748c3a9cf86c` | course_id 字段；非空；外键 → experiment_course.id |
| `project_id` | `UUID` | 是 | `1e20a175-3274-51ff-8473-023635609404` | project_id 字段；非空；外键 → experiment_project.id |
| `session_id` | `UUID` | 否 | `76b767a6-622e-5c4e-89e1-8127b6094772` | session_id 字段；外键 → experiment_session.id |
| `requirement_type` | `VARCHAR(20)` | 是 | `REQUIRED` | requirement_type 字段；非空 |
| `status` | `VARCHAR(20)` | 是 | `SELECTED` | status 字段；非空 |
| `selected_at` | `TIMESTAMPTZ` | 否 | `NULL` | selected_at 字段 |
| `withdrawn_at` | `TIMESTAMPTZ` | 否 | `NULL` | withdrawn_at 字段 |
| `completed_at` | `TIMESTAMPTZ` | 否 | `NULL` | completed_at 字段 |
| `absence_reason` | `TEXT` | 否 | `NULL` | absence_reason 字段 |
| `report_status` | `VARCHAR(20)` | 是 | `PENDING` | report_status 字段；非空 |
| `version_no` | `INTEGER` | 是 | `1` | version_no 字段；非空 |
| `created_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | created_by 字段 |
| `updated_by` | `UUID` | 否 | `fbb0e421-23bf-53d2-8b08-c0b6a5400d8e` | updated_by 字段 |
| `id` | `UUID` | 是 | `1f4705ca-2af4-5bfb-8763-ee950b4610ff` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：report_status IN ('NOT_REQUIRED', 'PENDING', 'SUBMITTED', 'PASSED', 'REJECTED')
- 检查：status IN ('AVAILABLE', 'SELECTED', 'COMPLETED', 'ABSENT', 'WITHDRAWN', 'MAKEUP_PENDING')
- 检查：requirement_type IN ('REQUIRED', 'OPTIONAL')
- 索引：ix_student_project_record_session_status
- 索引：ix_student_project_record_student_term
- 唯一索引：uq_student_project_active

## 41. 审批记录（`approval_record`）

| 字段名 | PostgreSQL 类型 | 必填 | 范例值 | 含义/约束 |
|---|---|:---:|---|---|
| `application_id` | `UUID` | 是 | `51ad1c3f-2b54-5785-8fda-45e8b22014c0` | application_id 字段；非空；外键 → application_request.id |
| `approval_type` | `VARCHAR(20)` | 是 | `AUTO` | approval_type 字段；非空 |
| `approver_user_id` | `UUID` | 否 | `494ddc8d-8954-558c-a7c2-1d6625a67038` | approver_user_id 字段；外键 → user_account.id |
| `decision` | `VARCHAR(20)` | 是 | `APPROVED` | decision 字段；非空 |
| `matched_rules` | `JSONB` | 是 | <code>{"demo": true}</code> | matched_rules 字段；非空 |
| `comment` | `TEXT` | 否 | `NULL` | comment 字段 |
| `decided_at` | `TIMESTAMPTZ` | 是 | `DEMO_VALUE` | decided_at 字段；非空 |
| `id` | `UUID` | 是 | `25dce4ea-3415-5878-94a1-b4887fbfd2f8` | id 字段；主键；非空 |
| `created_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | created_at 字段；非空 |
| `updated_at` | `TIMESTAMPTZ` | 是 | `2026-02-20 09:00:00+08` | updated_at 字段；非空 |

**表级约束**

- 检查：decision IN ('APPROVED', 'REJECTED', 'TRANSFERRED')
- 检查：approval_type IN ('AUTO', 'MANUAL')
- 索引：ix_approval_record_application

## 42. 教师正式实验课表索引（`teacher_timetable_entry`）

该表初始为空，只索引正式发布课表中的实验场次。课程、项目、实验室和时间信息通过 `experiment_session` 查询。

| 字段名 | PostgreSQL 类型 | 必填 | 含义/约束 |
|---|---|:---:|---|
| `teacher_id` | `UUID` | 是 | 教师；外键 → `teacher.id` |
| `term_id` | `UUID` | 是 | 学期；外键 → `academic_term.id` |
| `schedule_version_id` | `UUID` | 是 | 正式课表版本；外键 → `schedule_version.id` |
| `experiment_session_id` | `UUID` | 是 | 正式实验场次；外键 → `experiment_session.id` |
| `id` | `UUID` | 是 | 主键 |
| `created_at` | `TIMESTAMPTZ` | 是 | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 是 | 更新时间 |

约束：场次唯一；教师、学期和版本必须与场次一致；课表版本必须为 `PUBLISHED`。

## 43. 课程单节空闲人数（`course_time_availability`）

按“课程 + 学期 + 周次 + 星期 + 单节”保存当前聚合统计，时间网格与 `student_busy_bitmap` 一致。该数据只用于软评分，不作为学生无冲突的硬约束。

| 字段名 | PostgreSQL 类型 | 必填 | 含义/约束 |
|---|---|:---:|---|
| `course_id` | `UUID` | 是 | 实验课程；外键 → `experiment_course.id` |
| `term_id` | `UUID` | 是 | 学期；外键 → `academic_term.id` |
| `week_no` | `SMALLINT` | 是 | 教学周 |
| `day_of_week` | `SMALLINT` | 是 | 星期，1=周日、2=周一、…、7=周六 |
| `slot_no` | `SMALLINT` | 是 | 单节编号 |
| `target_student_count` | `INTEGER` | 是 | 课程目标学生总数 |
| `known_student_count` | `INTEGER` | 是 | 有兼容忙闲位图的学生数 |
| `free_student_count` | `INTEGER` | 是 | 本节空闲人数 |
| `busy_student_count` | `INTEGER` | 是 | 本节忙碌人数 |
| `unknown_student_count` | `INTEGER` | 是 | 位图缺失或不兼容人数 |
| `free_ratio` | `NUMERIC(8,6)` | 是 | 空闲人数占目标人数比例 |
| `data_coverage_ratio` | `NUMERIC(8,6)` | 是 | 已知忙闲数据覆盖率 |
| `mapping_version` | `INTEGER` | 是 | 聚合时间映射版本 |
| `calculation_version` | `INTEGER` | 是 | 本课程学期的计算版本 |
| `calculation_batch_id` | `UUID` | 是 | 本次刷新批次 |
| `source_hash` | `VARCHAR(128)` | 是 | 课程范围、学生和位图来源哈希 |
| `calculated_at` | `TIMESTAMPTZ` | 是 | 统计时间 |

全系统时间网格以周日为每周第一天、周六为最后一天，因此周末集合固定为 `{1, 7}`，工作日为 `{2, 3, 4, 5, 6}`。

排课 V2 草稿规则中的 `COURSE_EARLY_WEEK_PREFERENCE` 基础权重为 `0`。
管理员明确提出“某课程尽量安排在前 X 周”时，课程 ID 与
`preferred_end_week` 只进入本次 `schedule_job.input_snapshot`；
评分指标为目标课程晚于截止周的场次比例，不形成永久课程限制。

项目级需求使用独立的 `PROJECT_EARLY_WEEK_PREFERENCE` 规则，基础权重同样
为 `0`。项目 ID 与 `preferred_end_week` 只进入本次任务快照，评分指标为
目标项目晚于截止周的场次比例，不扩展影响同课程的其他项目。
| `id` | `UUID` | 是 | 主键 |
| `created_at` | `TIMESTAMPTZ` | 是 | 创建时间 |
| `updated_at` | `TIMESTAMPTZ` | 是 | 更新时间 |

约束：`free + busy = known`，`known + unknown = target`；同一课程、学期、周、星期和单节唯一。

<!-- metadata-summary: tables=41; columns=505 -->
